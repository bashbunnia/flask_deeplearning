name: CI tests

on: [push, pull_request]

jobs:
  tests:
    runs-on: ubuntu-22.04
    strategy:
      matrix:
        python: [ "3.10" ]

    steps:
      - uses: actions/checkout@v3
      - name: Load .env file
        uses: xom9ikk/dotenv@v2
        with:
          path: .
          mode: test
      - name: 